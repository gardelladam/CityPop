import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  placeHolder: {
    flex: 1,
    marginTop: "30%",
    padding: 50,
    alignItems: "center",
  },
  text: {
    fontSize: 20,
  },
});
export default styles;
