import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  center: {
    //alignItems: "center",
  },
  backButton: {
    width: "50%",
    padding: 5,
  },
});

export default styles;
